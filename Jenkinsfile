pipeline {
agent any
    stages {
        stage('Maven Build') {
            steps {
                echo 'Maven Build'
            }
            stage("Configrue"){
                echo 'Configure'
            }

        }
    }
}
